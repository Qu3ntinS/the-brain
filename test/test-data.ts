export const TEST_USERNAMES = ['test-user', 'docs-user'] as const

/** Profile patch applied in users.test.ts — revert after tests if still set. */
export const TEST_ADMIN_DISPLAY_NAME = 'Brain Admin'
